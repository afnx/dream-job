services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - PORT=5000
    env_file:
      - .env
    command: npm run dev

volumes:
  node_modules: